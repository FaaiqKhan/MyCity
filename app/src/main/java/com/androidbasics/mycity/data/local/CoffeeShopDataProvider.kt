package com.androidbasics.mycity.data.local

import com.androidbasics.mycity.data.*

object CoffeeDataProvider {
    val coffees = listOf(
        Coffee(type = CoffeeType.HOT, size = CoffeeSize.SMALL, price = )
    )
}